---
modules:
  - type: script
    snippets:
      - "dnf install https://s3.amazonaws.com/session-manager-downloads/plugin/latest/linux_64bit/session-manager-plugin.rpm -y --setopt=tsflags=noscripts"
      - "cp /usr/local/sessionmanagerplugin/bin/session-manager-plugin /usr/bin/"
